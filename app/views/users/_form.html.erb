<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if @user.new_record? %>
    <div class="field">
        <%= f.label :username %><br />
        <%= f.text_field :username %>
    </div>
    <% end %>
    <div class="field">
        <%= f.label :realname %><br />
        <%= f.text_field :realname %>
    </div>
    <div class="field">
        <%= f.label :email %><br />
        <%= f.text_field :email %>
    </div>
  
  <div class="field">
    <% if @user.new_record? %>
        <%= f.label :password %>
    <% else %>
      <strong>Change password (leave blank if you don't want to change)</strong><br>
        <%= f.label 'New password' %>
    <% end %>
    <br />
    <%= f.password_field :password %>
  </div>
  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation  %>
  </div>
  <div class="field">
    <%= f.label "Get e-mail notifications" %> <br />
    <%= f.check_box :subscribed %> <br />
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
